/*Problema 1001: Extremamente básico*/

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int A,B, soma;
    scanf("%d %d", &A, &B);
    soma = A + B;
    printf("X = %d\n", soma);
    return 0;
    
}